spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/swim
    username: swim_user
    password: swim_pass
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: debug
